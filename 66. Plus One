class Solution {
public:
    vector<int> plusOne(vector<int>& digits) {
        int n =  digits.size(); 
 
   for(int i=0;i<n;i++){
  if (n-1==i){
      
   digits[i] = digits[i]+1;

  }
  else cout<<digits[i];

   }
   
   return digits;  }
};
