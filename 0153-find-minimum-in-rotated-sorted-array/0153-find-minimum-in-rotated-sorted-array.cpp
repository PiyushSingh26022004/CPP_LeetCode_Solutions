class Solution {
public:
    int findMin(vector<int>& nums) {
        int k;
        sort(nums.begin(),nums.end());
        for(int i=0;i<1;i++)
            k = nums[i];
           return k;
    }
   
};